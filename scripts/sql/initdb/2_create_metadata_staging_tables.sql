CREATE TABLE IF NOT EXISTS us_states_staging
(
  states_rows   VARCHAR(50)     NOT NULL
);

CREATE TABLE IF NOT EXISTS countries_staging
(
  countries_rows   VARCHAR(64)  NOT NULL
);

CREATE TABLE IF NOT EXISTS stations_staging
(
  stations_rows   VARCHAR(85)  NOT NULL
);
